{
  "name": "claude-session-guard",
  "version": "0.1.0",
  "description": "Detects Claude Code usage-limit stops across all sessions and re-opens each one in tmux when the limit refreshes.",
  "type": "module",
  "bin": {
    "csg": "bin/csg.js"
  },
  "scripts": {
    "test": "node --test 'test/*.test.js'"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
